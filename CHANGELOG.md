## 0.0.2

- Added `flutter_ci doctor` for environment diagnostics.
- Added `flutter_ci init` for configuration scaffolding.
- Added `flutter_ci list` for browsing previous builds.
- Added `--flavor` support for build commands.
- Added automatic build output logging to `build.log`.
- Enhanced security with environment variable interpolation in `flutter_ci.yaml`.
- Added GitHub Actions example and Pub.dev badges to README.
- Upgraded dependencies for improved stability and pub points.

## 0.0.1

- Initial release.
- Added build and bump commands.
- Integrated Android and iOS build support.
- Added artifact storage service to local storage.
